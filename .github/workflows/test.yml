name: Canchu

on: 
 push:
   branches:
     - main
 workflow_dispatch:

permissions:
  contents: write

jobs:    
  build:
    runs-on: ubuntu-latest
    steps:
     - name: Checkout code
       uses: actions/checkout@v3
     - name: docker-compose build
       run: |
        pwd
        ls
        cd api/1.0
        docker-compose up --build -d
